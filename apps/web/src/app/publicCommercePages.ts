// A página pública de lojas (/loja) segue desativada — decisão comercial.
export const PUBLIC_STORE_PAGE_ENABLED = false;

// Área editorial pública (/guias, /guias/[slug]) — REATIVADA (set. 2026),
// início da estratégia Amazon Associates Brasil do PETMOL. A central de
// Guias volta ao ar como conteúdo editorial para tutores de cães e gatos,
// com uma seção de "Produtos selecionados pelo PETMOL" ainda em preparação
// (Fase 1: placeholders editoriais, sem produtos nem links de afiliado).
// A /recommendations (Amazon Associates US, em inglês) continua no ar por
// URL direta — só saiu do destaque do cabeçalho.
export const PUBLIC_GUIDES_PAGE_ENABLED = true;
export const PUBLIC_GUIDE_DETAIL_PAGE_ENABLED = true;
